#pragma once

namespace OS {
    /// \brief the list of provided operating systems
    /// \attention OS marked "OnlyForTests" not supported in real work system
    enum OSType {Windows, Linux, MacOS, OnlyForTestsGuaranteedInfectionOS, OnlyForTestsRandomOS};
}
